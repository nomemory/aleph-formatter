apply plugin: 'java'
apply plugin: 'idea'

group 'net.andreinc'
version '0.0.1'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.4'
}

test {
    testLogging { events 'started', 'passed' }
}